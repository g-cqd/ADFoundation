/// Namespace for generic text algorithms: bounded edit distance (Levenshtein) and the substring /
/// window tokenizer kernels shared by fuzzy search and full-text indexing. Stdlib-only and
/// iterative (the edit-distance matrix is computed row-by-row, never recursively).
public enum ADFText {}
